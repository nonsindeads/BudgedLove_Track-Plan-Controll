# Infrastructure Notes

## Caddy Split (Pending Decision)

This repository must only contain BudgetLove application components.

Caddy routes for the following domains/services do **not** belong in this repo:

- `cloud.*`
- `dns.*`
- `git.*`
- `code-*`
- `retrogaming.*`

TODO: Entscheidung ausstehend — eigenes Infra-Repo oder lokaler Pfad außerhalb des Repos.
